name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build release app
        run: |
          scripts/build-app.sh
          scripts/validate-publishing.sh
